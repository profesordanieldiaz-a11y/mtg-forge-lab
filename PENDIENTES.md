# PENDIENTES — 01_MTG
_Auditoría 2026-06-18_

## 🐛 Bugs

- ~~**[media]** Clasificación de color multicolor / frame incorrecto~~ ✅ **RESUELTO 2026-06-22** — `marco_multicolor_vacio.png` creado (paleta oro/dual); cartas multicolor ya usan frame correcto. El maná híbrido/Phyrexiano es **irrelevante**: proyecto solo trabaja con Old School (93–03) y Mid School (95–03).

- ~~**[media]** `streamlit_app.py:7` — Import sin uso real `buscar_cartas_db`.~~ ✅ **RESUELTO 2026-06-22** — `buscar_cartas_db` ya no aparece en el import de `streamlit_app.py:7`. Verificado en código actual.

- **[baja]** `card_list_parser.py:33` (`_RE_PLAIN`) — **El parser "plain" puede tragarse líneas que empiezan por dígito y no son cartas.** _Parcialmente mitigado desde 2026-06-22_ (verificado 2026-07-14): las líneas 134-138 ya rechazan nombres de <3 caracteres y counts >99, lo que filtra basura tipo `240p` o `1631★`. Pero una línea como "4 copies recommended" sigue pasando (nombre de 18 chars, count 4). En el flujo actual es de bajo impacto porque las listas vienen de generadores propios. Fix restante: validar el nombre contra la DB local (`data/cards_*.json`) antes de aceptarlo en modo plain.

- ~~**[baja]** `make_cards_old_border.py:434` (`fetch_art_crop`) — El arte siempre se re-descarga de Scryfall.~~ ✅ **RESUELTO 2026-06-22** — `load_art(card_name, image_url)` en línea 367 primero busca `data/artworks/<nombre>.jpg` local y solo llama a `fetch_art_crop` si no existe. `make_card_old` usa `load_art` en línea 456.

- ~~**[baja]** `make_cards_old_border.py:577-579` — Selección frágil de sección JSON con `or` (truthiness).~~ ✅ **RESUELTO 2026-06-22** — Líneas 602-607 ya usan `"mainboard" in entries_raw` (comprobación de clave) en lugar de truthiness. `mainboard` vacío `[]` ya no salta al siguiente.

## ⚠️ Incoherencias / riesgos

- ~~**[media]** **`data/artworks/` sigue trackeado en git pese a la decisión de ignorarlo**~~ ✅ **RESUELTO 2026-07-23** (Merlin) — `git rm -r --cached data/artworks/` ejecutado: 5.702 JPGs fuera del índice, archivos locales intactos, commit + **push a `origin/master` hechos** (`5843b4c`, 2026-07-23, con OK de Daniel). Opcional aún: reescribir historia (BFG/`git filter-repo` + force push) para recuperar los ~548 MB del `.git`. Detalle original abajo. (detectado 2026-07-14, revisión Merlin). Las notas de sesión 2026-06-22 dan por hecho que `data/artworks/` se movió a `.gitignore` junto con los JSONs, pero `.gitignore` no des-trackea archivos ya commiteados: `git ls-files 'data/artworks/*'` devuelve **5.702 JPGs** aún en el índice (~156 MB en working tree; `.git` pesa 548 MB). Los 4 JSONs sí se destrackearon correctamente; solo los artworks quedaron a medias. **Fix (lo ejecuta Daniel):** `git rm -r --cached data/artworks/` + commit — los archivos locales quedan intactos y los nuevos ya se ignoran. Opcional: reescribir historia (`git filter-repo` o BFG) para recuperar los ~548 MB del `.git` (requiere force push a `mtg-forge-lab`).

- ~~**[media]** **Repositorio pesado: JSONs de datos versionados en git** (`cards_all_eras.json` 7 MB, `cards_mid_school.json` 6 MB, `cards_old_school.json` 1,3 MB, `mtg_translations_es.json` 1,6 MB). Son datos regenerables/caché e inflan el repo.~~ ✅ **RESUELTO** (verificado 2026-07-01, drift) — **decisión: no versionarlos** (son regenerables). Ya están fuera de git: `git ls-files "*.json"` solo lista config (`.claude/settings.json`, `.devcontainer/devcontainer.json`); los 4 datos están en `.gitignore` (`data/cards_*.json`, `data/mtg_translations_es.json`) y `git check-ignore` los confirma ignorados. **Regeneración documentada** en CLAUDE.md → "Flujo completo desde cero": `python download_card_database.py` regenera `data/cards_*.json`; el caché de traducciones `mtg_translations_es.json` lo reconstruye `generate_all_cards.py` bajo demanda (Scryfall ES → Google Translate). Solo faltaba cerrar el drift aquí.

- ~~**[media]** **`requirements.txt` sin versiones fijadas (`streamlit`, `Pillow`, `reportlab`, `requests`, `deep-translator`).**~~ ✅ **RESUELTO** (verificado 2026-07-01, nocturno) — `requirements.txt` ya trae rangos mayores acotados: `streamlit>=1.31,<2.0`, `Pillow>=10.0,<13.0`, `reportlab>=3.6,<5.0`, `requests>=2.28`, `deep-translator>=1.11`. Una actualización mayor de Streamlit/Pillow ya no rompe la app en silencio.

- ~~**[media]** **`CLAUDE.md` desactualizado respecto al entorno real (Linux).**~~ ✅ **RESUELTO 2026-06-28** — `make_land_cards.py` ya tiene `_find_font(...)` con fallback Linux completo (DejaVu/Liberation/FreeSans/Ubuntu) en líneas 114-147. Verificado en código: ninguna ruta es solo-Windows. Pendiente cosmético (no bloquea): actualizar README/CLAUDE.md con instrucciones Linux.

- ~~**[baja]** **Dependencias no declaradas.** `test_card_list_parser.py` documenta uso con `pytest` pero `pytest` no está en `requirements.txt`. `card_list_parser.py:169` usa `tkinter` (clipboard) que no siempre viene instalado en Linux headless.~~ ✅ **RESUELTO (drift, verificado 2026-07-23)** — `requirements.txt` ya trae el bloque `# Dev / testing (no necesario para ejecutar la app)` con `# pytest>=7.0` y la nota de `tkinter` (built-in, solo para `load_card_list_clipboard()`); `CLAUDE.md` § "Notas Linux" documenta además `sudo apt install python3-tk`. Sin cambio de código necesario.

- ~~**[baja]** `download_card_database.py:38-43` — **`legalities` se conserva en cada carta** pero ninguna parte del proyecto lo usa. Es uno de los campos más voluminosos de Scryfall y multiplica el tamaño de los JSON. Fix: quitar `legalities` (y revisar si `keywords`/`color_identity` se usan) de `CAMPOS`.~~ ✅ **RESUELTO 2026-07-01** — verificado en código actual: `CAMPOS` (`download_card_database.py:38-43`) ya no incluye `legalities`; `grep -rn "legalities" --include="*.py"` no arroja usos en el proyecto. Sin cambios de código necesarios (drift del pendiente).

- **[baja]** **`packages.txt` instala fuentes que no se usan como primera opción.** Declara `fonts-dejavu-core` y `fonts-liberation`; el código las busca, correcto. Coherente, solo verificar que el devcontainer (`pip3 install --user`) realmente las tenga disponibles tras `apt install`.

- ~~**[baja]** `streamlit_app.py:266-267` — **Dependencia de CDNs externos** (`fonts.googleapis.com`, `cdn.jsdelivr.net/npm/mana-font@latest`). El `@latest` puede cambiar sin aviso y romper iconos; además sin red no hay iconos de maná. Fix: fijar versión de `mana-font` y/o servir local.~~ ✅ **RESUELTO 2026-07-01** (verificado, drift) — `streamlit_app.py:267` ya usa `mana-font@0.14.0` pineado (no `@latest`). Ya estaba resuelto de una sesión previa (ver `📓 Notas MTG/README.md:18`); solo se cerraba el drift aquí.

## ✨ Mejoras

- ~~**[seguridad/robustez]** `streamlit_app.py:866-889` — La app ejecuta `subprocess.run([sys.executable, fabricador, "--input", ruta_fabricar])`. Está bien que use `sys.executable` y lista de args (no shell). Pero `ruta_fabricar` sale de un `selectbox` poblado por `os.listdir(DATA_DIR)`, así que es seguro. Mejora menor: añadir `timeout=` al `subprocess.run` para que un cuelgue de red no bloquee la UI indefinidamente.~~ ✅ **RESUELTO 2026-07-01** — verificado en código actual: `subprocess.run(...)` en `streamlit_app.py:878` ya trae `timeout=600`. Sin cambios de código necesarios (drift del pendiente).

- ~~**[rendimiento]** `streamlit_app.py:628`, `:777` y `:920-924` — Las traducciones se leen de disco varias veces por render. `_cargar_traducciones()` ya usa `@st.cache_resource` (bien), pero el bloque de la consola (`:920`) abre y parsea el JSON otra vez sin caché. Fix: reutilizar `_cargar_traducciones()`.~~ ✅ **RESUELTO 2026-07-01** — verificado en código actual: el bloque de la consola (`streamlit_app.py:922`) ya llama a `_cargar_traducciones()` en vez de abrir el JSON de nuevo. Sin cambios de código necesarios (drift del pendiente).

- ~~**[rendimiento]** `streamlit_app.py:631` — `_buscar_bilingue(..., max_results=500)` recorre toda la DB en Python por cada pulsación de tecla del buscador. Con ~5700 cartas es tolerable, pero para fluidez se podría indexar (precomputar minúsculas) o usar `@st.cache_data` sobre la query.~~ ✅ **RESUELTO 2026-07-01** — nueva función `_indice_busqueda_bilingue(era_key)` cacheada con `@st.cache_data` precomputa nombre/tipo/oracle/traducciones en minúsculas una sola vez por era (no por cada tecla). `_buscar_bilingue` ahora itera ese índice en paralelo con la DB (`zip`) en vez de hacer `.lower()` al vuelo. El índice cachea solo strings (no los dicts de carta completos) para evitar que Streamlit los pickle-copie en cada render. Mismo comportamiento verificado (mismos resultados para varias queries, incluido el corte por `max_results`).

- ~~**[robustez]** `translator.py` y `make_cards_old_border.py` — Las llamadas a Scryfall (`requests.get`) sin reintento/backoff (solo `download_card_database.py`/`deck_builder.py` lo tienen). Ante un 429 puntual, la traducción o el arte simplemente fallan en silencio. Fix: reusar `_request_with_backoff`.~~ ✅ **RESUELTO 2026-07-10 (nocturno)** — `translator.py` (`_fetch_scryfall_es`) y `make_cards_old_border.py` (`fetch_art_crop`, `fetch_scryfall`) ahora importan y usan `_request_with_backoff` de `deck_builder.py` (`from deck_builder import _request_with_backoff`) en vez de llamar a `requests.get` directo. Mismo comportamiento en éxito (mismo objeto de respuesta / mismo JSON); solo cambia el manejo de fallos transitorios (429 y errores de red ahora reintentan con backoff exponencial en vez de fallar en el primer intento). Verificado con `python3 -m py_compile` sobre ambos archivos e importación real de los módulos (`deck_builder`, `card_list_parser`, `translator`) sin errores; `make_cards_old_border.py` no se pudo importar de punta a punta en este entorno porque falta el paquete `reportlab` en el Python del sandbox (limitación de entorno, no relacionada con el fix).

- ~~**[estructura]** Hay **duplicación masiva de constantes** (dimensiones `OUT_W/OUT_H/PAD/IX/...`, `MANA_COL`, `COLOR_THEMES`, `rrect`, `place_art`, `art_shadow`, `fit_font`) repetidas en `make_cards_old_border.py`, `make_land_cards.py`, `generate_empty_frames.py`, `generate_land_frames.py`. Fix: extraer a un módulo común `card_layout.py`.~~ ✅ **RESUELTO 2026-07-23 (sesión autónoma /merlin)** — creado **`card_layout.py`** con dimensiones + zonas verticales, paleta `C`, `COLOR_THEMES`, `MANA_COL`/`mana_col`, `find_font`/`FONT_PATHS`/`fnt`/`text_w`/`fit_font` y las primitivas `rrect`/`place_art`/`art_shadow`. Los 4 archivos ahora importan de ahí (−235 líneas netas: `make_cards_old_border` 747→571, `make_land_cards` 314→228). `make_land_cards` importa `text_w as tw` para no tocar sus call sites.
  **Verificado que no cambia ni un píxel** (no por inspección): (a) por AST, antes de tocar nada, se comprobó que las copias eran subconjuntos **sin un solo valor en conflicto** — `C` y `MANA_COL` de los archivos pequeños eran subconjuntos exactos, `COLOR_THEMES` de `generate_empty_frames` solo omitía `tb_text` (que no usa), y `_FP` solo omitía `srfi`; (b) se generaron los **12 marcos** con la versión pre-refactor extraída de git (`git show HEAD:…`) en un directorio aparte y con la nueva → **byte-idénticos los 12** (`cmp`); (c) las tres variantes históricas de `place_art`/`art_shadow` (diferían en variables intermedias) se ejecutaron una contra otra sobre 4 tamaños de arte distintos → salida idéntica bit a bit; (d) `py_compile` de los 7 módulos del proyecto y chequeo AST de nombres sin definir → limpio. Los PNG de `pruebas/marcos_tierras/` que se regeneraron durante la verificación se restauraron con `git checkout` (el repo solo queda con cambios de código).

- ~~**[validación]** `make_cards_old_border.py:550` — Si `mtg_translations_es.json` no existe, el `open(...)` peta sin mensaje claro. Fix: comprobar existencia y dar instrucción (como hace `load_cards` en `generate_all_cards.py`).~~ ✅ **RESUELTO 2026-07-01** — verificado en código actual: `make_cards_old_border.py:572-576` ya comprueba `os.path.exists(trans_path)` y hace `sys.exit(...)` con instrucción de ejecutar `generate_all_cards.py` primero. Sin cambios de código necesarios (drift del pendiente).

- ~~**[ux]** `streamlit_app.py` Tab 3 — El log del subproceso solo se muestra al terminar (no streaming). Para procesos de varios minutos, el usuario no ve progreso. Mejora opcional: streaming de `stdout`.~~ ✅ **RESUELTO 2026-07-22 (nocturno)** — `subprocess.run` reemplazado por `Popen` + hilo lector + `Queue` (portable a Windows, sin `select` sobre pipes): las últimas 20 líneas del fabricador se muestran en vivo en un `st.code` mientras corre, y el expander final conserva el log completo. El timeout de 600 s se mantiene con deadline propio (`time.monotonic`) que mata el proceso **incluso si se queda mudo** (el `subprocess.run` original ya lo cubría; el bucle ingenuo de readline lo habría perdido); nuevo mensaje de error explícito "Tiempo agotado". `stderr` va a `STDOUT` (mismo log, orden real de emisión — antes se concatenaba al final). Verificado: sintaxis `ast.parse` OK + el patrón exacto del bucle probado aparte con 3 casos (salida incremental, fallo con returncode, proceso mudo matado a los 2 s) en `15_Merlin/data/test_streaming_mtg.py`, 3/3 OK. No se pudo levantar la UI de Streamlit en esta sesión (bash restringido a 15_Merlin) — humo visual pendiente para la próxima vez que Daniel fabrique cartas.

## ❓ Dudas / a confirmar con el usuario

> _Las 5 dudas quedaron **respondidas de facto** por el trabajo ya hecho, pero nunca se tacharon
> (drift documental). Cerradas el **2026-07-27 (nocturno)** tras verificar cada una contra el código
> y el árbol reales — ninguna requería ya decisión de Daniel._

1. ~~**JSONs de datos en git (7 MB + 6 MB + …):** ¿es intencional versionarlos, o prefieres ignorarlos y regenerarlos con `download_card_database.py`?~~ ✅ **RESUELTA** — decidido **no versionarlos** (ver ítem ✅ de "Incoherencias", verificado 2026-07-01): los 4 JSON están en `.gitignore` y la regeneración está documentada en CLAUDE.md. Los 5.702 artworks se destrackearon el 2026-07-23.
2. ~~**Frame multicolor:** ¿`marco_multicolor_vacio.png` propio (oro/dual) o las multicolor usan el frame `cafe`?~~ ✅ **RESUELTA — marco propio.** Verificado 2026-07-27: `assets/marcos/marco_multicolor_vacio.png` **existe**, y `make_cards_old_border.py:260-262` asigna `color = "multicolor"` a toda carta con 2+ colores y carga `marco_{color}_vacio.png` — carpeta y frame coinciden, la incoherencia con `cafe` ya no existe (`cafe` queda solo para incoloro/artefacto, línea 258).
3. ~~**Artworks locales:** ¿se llegó a usar `download_artworks.py`? El renderer nunca lee `data/artworks/`.~~ ✅ **RESUELTA — sí está conectado; la duda estaba desactualizada.** Verificado 2026-07-27: `load_art(card_name, image_url)` (`make_cards_old_border.py:221-229`) busca primero `data/artworks/<nombre_seguro>.jpg` y solo cae a `fetch_art_crop` (Scryfall) si no existe; `make_card_old` lo usa en la línea **285**. `generate_all_cards.py:7` lo documenta igual. La conexión se hizo el 2026-06-22 (ver ítem ✅ de Bugs) — la duda es anterior a ese fix. _(Nota: los números de línea del ítem tachado de Bugs —367/456— quedaron obsoletos tras el refactor de `card_layout.py`; los vigentes son 221/285.)_
4. ~~**Entorno objetivo:** ¿Linux (PC Gamer) o Windows (Legionario)?~~ ✅ **RESUELTA — Linux (PC Gamer Ubuntu).** Verificado 2026-07-27 en `CLAUDE.md`: la sección de comandos abre con "# Linux (PC Gamer Ubuntu)" (línea 11) y existe una sección "Notas Linux" (96-98) que documenta el fallback de fuentes DejaVu/Liberation/FreeSans en `make_cards_old_border.py` y `make_land_cards.py` ("No necesitan fuentes de Windows"). El arreglo de fuentes que motivaba la duda ya está hecho (ítem ✅ 2026-06-28).
5. ~~**`maná híbrido/Phyrexiano` en Old/Mid School:** ¿confirmas que solo trabajas con esas eras?~~ ✅ **RESUELTA — sí, solo Old School (93-03) y Mid School (95-03).** Ya registrado en el primer ítem ✅ de Bugs ("el maná híbrido/Phyrexiano es **irrelevante**") y respaldado por el código: `CLAUDE.md:18-24` solo ofrece `--era old` / `--era mid`, y `_folder_color` (`make_cards_old_border.py:252-260`) detecta color con `{[BWURGX]}` sin tratar híbridos, coherente con esa decisión.
